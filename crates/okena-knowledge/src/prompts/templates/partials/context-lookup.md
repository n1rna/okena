---
description: Tells an agent it can look up map entries, specs and knowledge itself, through okena's MCP tools
---
To find more context yourself — map entries, specs, knowledge docs, skills and agents from your projects and the knowledge stores they follow — search with `okena_context_search` and read a result with `okena_context_read`.
