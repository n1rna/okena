---
description: How to use the openspec CLI against a registered store
---
This is the OpenSpec store `{store_id}`. If the `openspec` CLI is installed you may use it; pass `--store {store_id}` so every command targets this store, e.g. `openspec status --change {change} --store {store_id}`. Do not install the CLI if it is not.
