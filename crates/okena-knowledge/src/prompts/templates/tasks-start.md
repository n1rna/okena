---
name: tasks-start
description: Brief one agent starting work on several tasks, each in worktrees of its own
for: tasks-start
---
Work on {key}, together.
{tasks}{note}

Every task has its own branch and its own worktrees, already created and checked out. Keep each task's change in that task's worktrees, on its branch, so each lands on its own: never make one task's change in another task's checkout. Read what is there before changing it, and keep each change to what its task asks for. Ask me about anything ambiguous rather than guessing at scope.{verify}

{>reporting}
