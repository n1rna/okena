name: Build

on:
  push:
    branches: ['**']
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_NET_RETRY: 10
  RUSTUP_MAX_RETRIES: 10

jobs:
  # Runs on every push: Linux check + tests
  check:
    if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.93"
          components: clippy

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: x86_64-unknown-linux-gnu

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libxcb1-dev \
            libxcb-render0-dev \
            libxcb-shape0-dev \
            libxcb-xfixes0-dev \
            libxkbcommon-dev \
            libxkbcommon-x11-dev \
            libwayland-dev \
            libvulkan-dev \
            libegl1-mesa-dev \
            libssl-dev \
            libfontconfig-dev \
            pkg-config \
            cmake

      # Test dependencies, not build ones: dtach backs the session-teardown
      # tests, zsh and fish the dropped-path quoting round-trip. Without them
      # those tests skip themselves and the job stays green having proved less.
      - name: Install test dependencies
        run: sudo apt-get install -y dtach zsh fish

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Build web client
        run: cd web && bun install --frozen-lockfile && bun run build

      - name: Check compilation
        run: cargo check --release

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Daemon/client split gates
        run: |
          scripts/audit-daemon-client-gaps.sh
          if cargo tree -p okena-daemon --edges no-dev | grep -E 'gpui|gpui-component'; then
            echo "okena-daemon must stay GPUI-free"
            exit 1
          fi

      # `cargo test` builds another package's bin target as a test harness, never
      # as a plain binary, so the end-to-end tests that drive the TUI would skip
      # themselves without this.
      - name: Build the TUI binary the end-to-end tests drive
        run: cargo build -p okena-tui

      # The workspace declares no `default-members`, so a bare `cargo test` would
      # run the root `okena` package alone and skip every member crate's tests.
      - name: Run tests
        env:
          # Turn a missing prerequisite from a silent skip into a failure.
          OKENA_REQUIRE_SHELLS: 1
          OKENA_REQUIRE_TUI: 1
        run: cargo test --workspace --no-fail-fast

  # Windows-only `cfg` branches never compile in the Linux `check` job above, so
  # a platform-specific break used to surface only once a tag started the full
  # matrix — too late, with the tag already pushed. Compile for Windows on every
  # push instead. Plain `cargo check`, not clippy: Windows still carries
  # pre-existing warnings that `-D warnings` would turn into failures.
  check-windows:
    if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.93"

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: x86_64-pc-windows-msvc-check

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      # okena-remote-server embeds web/dist through RustEmbed, so the assets
      # have to exist before the Rust build.
      - name: Build web client
        run: cd web && bun install --frozen-lockfile && bun run build

      - name: Check compilation
        run: cargo check --workspace --all-targets

  # React Native mobile client — TypeScript checks.
  #
  # The native app build (uniffi-bindgen-react-native cross-compiling
  # crates/okena-mobile-ffi to an Android NDK .so / iOS xcframework, then
  # `react-native run-*`) needs the mobile toolchain and is NOT yet wired into
  # CI — see mobile/rn/README.md for the device-side steps. Until then CI
  # type-checks, lints and Jest-tests the RN/TS sources; the Rust FFI crate is
  # already covered by the `check` job's workspace build + clippy + tests.
  mobile-rn:
    if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile/rn
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mobile/rn/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

  # Full multi-platform build: tags + manual trigger
  build:
    if: ${{ startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: okena-linux-x64
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact: okena-macos-arm64
          - os: macos-15-intel
            target: x86_64-apple-darwin
            artifact: okena-macos-x64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: okena-windows-x64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.93"
          targets: ${{ matrix.target }}

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ${{ matrix.target }}
          save-if: ${{ startsWith(github.ref, 'refs/tags/v') }}

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libxcb1-dev \
            libxcb-render0-dev \
            libxcb-shape0-dev \
            libxcb-xfixes0-dev \
            libxkbcommon-dev \
            libxkbcommon-x11-dev \
            libwayland-dev \
            libvulkan-dev \
            libegl1-mesa-dev \
            libssl-dev \
            libfontconfig-dev \
            pkg-config \
            cmake

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Build web client
        run: cd web && bun install --frozen-lockfile && bun run build

      - name: Build
        run: cargo build --release --target ${{ matrix.target }} -p okena -p okena-daemon

      - name: Prepare artifact (Linux)
        if: runner.os == 'Linux'
        run: |
          mkdir -p dist/icons
          cp target/${{ matrix.target }}/release/okena dist/
          chmod +x dist/okena
          # Ship the GPUI-free daemon sibling so the app runs the real daemon
          # binary instead of falling back to `okena --headless`.
          cp target/${{ matrix.target }}/release/okena-daemon dist/
          chmod +x dist/okena-daemon
          # Bundle icons for Linux installation
          cp assets/app-icon-16.png assets/app-icon-32.png assets/app-icon-48.png \
             assets/app-icon-64.png assets/app-icon-128.png assets/app-icon-256.png \
             assets/app-icon-512.png dist/icons/
          cp okena.desktop dist/

      - name: Create macOS app bundle
        if: runner.os == 'macOS'
        run: |
          chmod +x scripts/bundle-macos.sh
          ./scripts/bundle-macos.sh --target ${{ matrix.target }} --skip-build --dmg

      - name: Prepare artifact (Windows)
        if: runner.os == 'Windows'
        run: |
          mkdir dist
          copy target\${{ matrix.target }}\release\okena.exe dist\
          # Ship the GPUI-free daemon sibling (see Linux step).
          copy target\${{ matrix.target }}\release\okena-daemon.exe dist\

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/
          retention-days: 7

  # Create release when a tag is pushed
  release:
    needs: [build]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # A tag with a semver pre-release identifier (v1.0.0-beta.1) must ship as a
      # GitHub prerelease: the in-app updater and both install scripts read
      # /releases/latest, which skips prereleases, so this is what keeps a beta
      # from reaching everyone on the stable channel.
      - name: Classify release
        id: meta
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          if [[ "$VERSION" == *-* ]]; then
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
          else
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      # upload-artifact drops permission bits (files come back 0644), and the ZIP
      # and TAR below both record modes — so the exec bit has to be restored here,
      # between the download and the archiving, or it ships missing.
      - name: Restore executable bits
        run: |
          chmod +x artifacts/okena-linux-*/okena artifacts/okena-linux-*/okena-daemon
          chmod +x artifacts/okena-macos-*/Okena.app/Contents/MacOS/okena \
                   artifacts/okena-macos-*/Okena.app/Contents/MacOS/okena-daemon

      - name: Prepare release assets
        run: |
          cd artifacts
          for dir in */; do
            name="${dir%/}"
            if [[ "$name" == *android* ]]; then
              # Move APKs to release root
              mv "$dir"/*.apk ../
            elif [[ "$name" == *ios* ]]; then
              # Move iOS zip to release root
              mv "$dir"/*.zip ../
            elif [[ "$name" == *windows* ]]; then
              zip -r "../${name}.zip" "$dir"
            elif [[ "$name" == *macos* ]]; then
              # macOS: DMG is already created, just move it
              mv "$dir"/*.dmg "../" 2>/dev/null || true
              # Also create a zip of the .app for those who prefer it
              cd "$dir" && zip -r "../../${name}.zip" "Okena.app" && cd ..
            else
              tar -czvf "../${name}.tar.gz" -C "$dir" .
            fi
          done

      # Guards the step above: the shell installer and the in-app updater both
      # unpack these archives directly, so a lost exec bit is a broken install.
      - name: Assert executable bits in release archives
        run: |
          failed=0
          assert_exec() {
            local archive="$1" entry="$2" mode
            case "$archive" in
              *.tar.gz) mode=$(tar -tvzf "$archive" | awk -v f="$entry" '$NF == f { print $1; exit }') ;;
              *) mode=$(unzip -Z "$archive" "$entry" 2>/dev/null | awk 'NR == 1 { print $1 }') ;;
            esac
            case "$mode" in
              -*x*) echo "ok    $archive :: $entry ($mode)" ;;
              *) echo "FAIL  $archive :: $entry (mode '${mode:-not found}')"; failed=1 ;;
            esac
          }

          shopt -s nullglob
          linux_archives=(okena-linux-*.tar.gz)
          macos_archives=(okena-macos-*.zip)
          [[ ${#linux_archives[@]} -gt 0 ]] || { echo "FAIL  no okena-linux-*.tar.gz produced"; failed=1; }
          [[ ${#macos_archives[@]} -gt 0 ]] || { echo "FAIL  no okena-macos-*.zip produced"; failed=1; }

          for archive in "${linux_archives[@]}"; do
            assert_exec "$archive" ./okena
            assert_exec "$archive" ./okena-daemon
          done
          for archive in "${macos_archives[@]}"; do
            assert_exec "$archive" Okena.app/Contents/MacOS/okena
            assert_exec "$archive" Okena.app/Contents/MacOS/okena-daemon
          done

          exit "$failed"

      # The in-app updater looks for an asset named exactly `SHA256SUMS` and
      # silently skips verification when it is absent (checker.rs:204).
      - name: Generate SHA256SUMS
        run: |
          shopt -s nullglob
          assets=(*.tar.gz *.zip *.dmg *.apk)
          [[ ${#assets[@]} -gt 0 ]] || { echo "no release assets to checksum"; exit 1; }
          sha256sum "${assets[@]}" > SHA256SUMS
          cat SHA256SUMS

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          prerelease: ${{ steps.meta.outputs.prerelease }}
          files: |
            *.tar.gz
            *.zip
            *.dmg
            *.apk
            SHA256SUMS
          generate_release_notes: true
